#pragma once
// Forwarding shim (org-pass firesale phase 4b): moved to codec/serialization/HavokFile.h
// (pure byte<->disk I/O, no Havok knowledge — was misfiled under sct). Delete when havok-core is cut.
#include <codec/serialization/HavokFile.h>
