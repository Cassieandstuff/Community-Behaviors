#pragma once
// Forwarding shim (org-pass firesale phase 1b): moved to codec/serialization/HkyArchive.h.
// Delete when havok-model consumers migrate (phase 3).
#include <codec/serialization/HkyArchive.h>
