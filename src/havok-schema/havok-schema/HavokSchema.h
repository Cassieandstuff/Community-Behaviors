#pragma once
// Forwarding shim (org-pass firesale phase 2): the descriptor registry moved to
// interface/reflection/. Delete when consumers migrate to <interface/reflection/HavokSchema.h>.
#include <interface/reflection/HavokSchema.h>
