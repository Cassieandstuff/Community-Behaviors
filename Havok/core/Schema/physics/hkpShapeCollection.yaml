name:      hkpShapeCollection
parent:    hkpShape
signature: 0x00000000   # abstract base; sig unused on read, placeholder
size:      40
# hkpShape ends @32. m_disableWelding (bool) + m_collectionType (enum hkUint8), pad to 40.
fields:
  - { name: disableWelding, type: bool }   # @32
  - { name: collectionType, type: int8 }   # @33  (enum CollectionType)
  - { skip: 6 }                            # pad -> 40
