name:      hkpConvexVerticesConnectivity
parent:    hkReferencedObject
signature: 0x00000000   # read-only class (creature ragdoll shape); sig unused on read, placeholder
size:      48
# hkReferencedObject ends @16. Two hkArrays (16-byte headers): m_vertexIndices (uint16),
# m_numVerticesPerFace (uint8). Array CONTENTS are inlined after the struct (real object ~112).
fields:
  - { name: vertexIndices,      type: scalararray, scalar: uint16 }  # @16
  - { name: numVerticesPerFace, type: scalararray, scalar: byte }    # @32 -> 48
