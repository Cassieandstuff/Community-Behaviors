name:      hkaSkeletonLocalFrameOnBone
parent:    null
signature: 0x052e8043
size:      16
fields:
  - { empty: ptr }                   # hkLocalFrame — not ported; pointer dropped
  - { name: boneIndex, type: int32 }
  - { skip: 4 }
